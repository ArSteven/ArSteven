name: Chess
on:
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  chess:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: tim-mango/github-chess-action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
